# Proxmox settings
pve_host: "192.168.0.100"
pve_node: "pve"
pve_user: "root@pam"
pve_token_id: "maintenance"

# Misc
dockge_stacks_path: "/opt/stacks"

# Number of pre-update snapshots to keep per guest (used by snapshot_proxmox_machines)
snapshot_retention: 2
